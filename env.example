# ENV PLACEHOLDERS (do not commit real secrets)
DATABASE_URL="file:./dev.db"
NEXT_PUBLIC_APP_NAME="Anti-Fraud Platform"

# Admin-only token for bootstrap endpoints (server-side only; never client)
ADMIN_TOKEN=
